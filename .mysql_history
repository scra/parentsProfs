/*Création de la table Administrateur*/
CREATE TABLE Administrateur
(
idAdministrateur INTEGER(10) PRIMARY KEY,
identifiant VARCHAR(20),
motDePasse VARCHAR(20),
niveauDeDroit VARCHAR(3) /* A DEFINIR LES DIFFÉRENTS DROIT || par défault il y a 3 rang*/ 
);
CREATE TABLE Administrateur ( idAdministrateur INTEGER(10) PRIMARY KEY, identifiant VARCHAR(20), motDePasse VARCHAR(20), niveauDeDroit VARCHAR(3)   );
use c9
source CreateTable.sql
CREATE TABLE test();
CREATE TABLE test(test VARCHAR(10));
source CreateTable.sql
drop table Administrateur
Select * 
;
show table;
use show tables;
use c9
show tables;
drop table Administrateur;
CREATE TABLE Administrateur
(
idAdministrateur INTEGER(10) PRIMARY KEY,
identifiant VARCHAR(20),
motDePasse VARCHAR(20),
niveauDeDroit VARCHAR(3) /* A DEFINIR LES DIFFÉRENTS DROIT || par défault il y a 3 rang*/ 
show tables;
use c9
source CreateTable.sql
show tables;
source CreateTable.sql
show tables;
$ phpmyadmin-ctl install
select * 
dd
show databases;
alter table Reunion ADD nom VARCHAR(30);
use c9
alter table Reunion ADD nom VARCHAR(30);
use c9
select * from Administrateur;
show tables;
show Administrateur;
show Administrateur
;
select * from Administrateur;
SHOW COLUMN FROM Administrateur;
echo $id
;
